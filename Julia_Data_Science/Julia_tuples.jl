# Julia tuples

# Tuples are a Julia type
# tuples are defined using brackets () and commas in Julia
# Inside the tuples, the elements can be Str, Num and even array, another tuple


tup1 = (3.0, 9.1, 0.8, 1.9) 
print(tup1)